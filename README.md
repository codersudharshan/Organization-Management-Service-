# Organization-Management-Service-
Backend: Organization Management Service — FastAPI + Motor + MongoDB. Implements org creation, dynamic per-org collections, admin JWT auth, rename/update/delete flows, tests and Docker.
